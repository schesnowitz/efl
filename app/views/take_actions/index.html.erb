<p id="notice"><%= notice %></p>

<h1>Take Actions</h1>

<table>
  <thead>
    <tr>
      <th>Hide take action</th>
      <th>Take action string 1</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @take_actions.each do |take_action| %>
      <tr>
        <td><%= take_action.hide_take_action %></td>
        <td><%= take_action.take_action_string_1 %></td>
        <td><%= link_to 'Show', take_action %></td>
        <td><%= link_to 'Edit', edit_take_action_path(take_action) %></td>
        <td><%= link_to 'Destroy', take_action, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Take Action', new_take_action_path %>
